# Module 8: Billing — Backend Tasks

**Branch:** `feature/billing-module`

### Task 1: Invoice Schema and Repository
- [x] 1.1–1.3 Create schema and repository with unit tests (status `DRAFT`/`ISSUED`/`PAID`/`PARTIALLY_REFUNDED`/`REFUNDED`/`CANCELLED`; references patient/session/catalog; sequential invoice number per clinic)

### Task 2: Payment Record Schema and Repository
- [x] 2.1–2.3 Create schema and repository with unit tests — records **amount + free-text method label** (`CASH`/`CARD`/`BANK_TRANSFER`/`OTHER`) only. NO card numbers or bank-account details; the platform does not process payments.

### Task 3: Invoice Service
- [x] 3.1–3.5 Implement generateInvoice (manual "Generate Invoice"; optionally from a session — pre-populate appointment SERVICE + recommended catalog items), addLineItem, issue (`DRAFT → ISSUED`), markPaid (`ISSUED → PAID`, with method label), cancel (`DRAFT`/`ISSUED → CANCELLED`; regenerate instead of editing), totals recalculation
- [x] 3.6 Write unit tests

### Task 4: Refund Service
- [x] 4.1–4.3 Implement recordRefund against `ISSUED`/`PAID` invoices (full → `REFUNDED`, partial → `PARTIALLY_REFUNDED`; cumulative ≤ total), unit tests

### Task 5: Invoice Generation Trigger (manual)
- [x] 5.1–5.3 Invoices are generated by a manual staff action — Billing consumes **no events** to create invoices (no auto-generate on `SessionCompleted`, no `TreatmentPlanSigned` handler). Signed Treatment Plan/Prescription content MAY be used to suggest line items at generation time only.

### Task 6: Billing Resolvers
- [x] 6.1–6.5 Create resolvers, DTOs, integration tests

### Task 7: Billing Module Registration
- [x] 7.1–7.4 Create module, wire into AppModule, barrel export
