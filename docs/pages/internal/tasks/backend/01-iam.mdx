# Module 1: Auth & IAM — Backend Tasks

**Branch:** `feature/auth-iam`

### Task 1: IAM Error Codes and Domain Errors
- 1.1 Add 18 IAM-specific error codes
- 1.2 Create 14 domain error subclasses
- 1.3 Write unit tests

### Task 2: AuthSession Schema and Repository
- 2.1–2.2 Create AuthSession schema and repository
- 2.3 Write unit tests
- 2.4 Write property test: revokeAllForStaff leaves no ACTIVE sessions

### Task 3: PasswordResetToken Schema and Repository
- 3.1–3.3 Create schema, repository, and unit tests

### Task 4: Password Hashing Utilities
- 4.1 Create hashPassword/verifyPassword with argon2id
- 4.2–4.3 Write unit and property tests

### Task 5: JWT Service
- 5.1–5.3 Create JwtService with RS256 sign/verify
- 5.4–5.5 Write unit tests including algorithm enforcement

### Task 6: Auth Service - Login
- 6.1–6.4 Implement login with audit, failure cases, no user enumeration
- 6.5–6.6 Write unit tests
- 6.7 Write property test: never returns tokens for non-ACTIVE staff

### Task 7: Auth Service - Refresh
- 7.1–7.4 Implement refresh with token rotation, stolen token detection, staff status check
- 7.5–7.7 Write unit tests
- 7.8 Write property test: old refresh token invalid after rotation

### Task 8: Auth Service - Logout
- 8.1–8.5 Implement logout with single-session revocation, unit tests

### Task 9: Auth Service - Password Reset
- 9.1–9.3 Implement requestPasswordReset and resetPassword
- 9.4–9.5 Write unit tests
- 9.6 Write property test: all sessions revoked after reset

### Task 10: Auth Resolver
- 10.1–10.4 Create GraphQL resolver with HTTP-only cookies, DTOs
- 10.5–10.6 Write integration tests

### Task 11: Auth Module Registration
- 11.1–11.3 Create AuthModule with exports and barrel

### Task 12: Staff Schema and Repository
- 12.1–12.3 Create Staff schema and repository
- 12.4 Write property test: tenant scoping

### Task 13: Role Schema and Repository
- 13.1–13.3 Create Role schema and repository with unit tests

### Task 14: InviteToken Schema and Repository
- 14.1–14.3 Create InviteToken schema and repository with unit tests

### Task 15: Password Policy Service
- 15.1–15.2 Create PasswordPolicyService with validation rules
- 15.3 Write property test: validation correctness

### Task 16: Invite Service
- 16.1–16.5 Implement sendInvite, acceptInvite, resendInvite, cancelInvite
- 16.6–16.8 Write unit tests
- 16.9–16.10 Write property tests: PENDING_REGISTRATION status, 7-day expiry

### Task 17: Staff Service
- 17.1–17.4 Implement staff CRUD, deactivation, reactivation
- 17.5–17.7 Write unit tests

### Task 18: Last Admin Guard
- 18.1–18.3 Create LastAdminGuard with unit tests
- 18.4 Write property test: blocked when count is 1

### Task 19: Role Service
- 19.1–19.4 Implement role CRUD with system role protection, OrgAdmin scope violation
- 19.5–19.7 Write unit tests

### Task 20: Access Resolution Engine
- 20.1–20.6 Implement permission resolution, OrgAdmin restrictions, dual-role, deny-by-default
- 20.7–20.9 Write unit tests
- 20.10–20.11 Write property tests: always returns ALLOWED/DENIED, effective permissions = union

### Task 21: Plan Gate Service
- 21.1–21.3 Create stub PlanGateService with unit tests

### Task 22: Visibility Interceptor
- 22.1–22.3 Create VisibilityInterceptor with unit tests

### Task 23: IAM Resolvers
- 23.1–23.6 Create Staff, Role, Invite resolvers with DTOs and @RequirePermission
- 23.7–23.8 Write integration tests

### Task 24: Self-Registration
- 24.1–24.4 Create RegistrationService and resolver
- 24.5 Write unit tests
- 24.6 Write property test: creates exactly 1 org + 1 clinic + 1 staff

### Task 25: IAM Module Registration
- 25.1–25.4 Create IamModule, wire into AppModule, barrel export

### Task 26: Seed Data
- 26.1–26.3 Create system roles seed (OrgAdmin, ClinicAdmin) + default roles (Doctor, Receptionist, Nurse, Sales, Marketing, Frontdesk)
- 26.4 Write unit tests verifying idempotency
